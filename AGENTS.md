# AGENTS.md — sub-12

The project instructions live in **[CLAUDE.md](CLAUDE.md)**. Read that file.

It is the single source of truth for the repository layout, build and test
commands, migration rules, code conventions, API structure, environment
variables, the CI pipeline and the design system.

This file used to carry a second copy of all of it. The copy drifted, and a
stale instruction is worse than none — it was still describing club deletion as
platform-admin-only and had no idea events existed. So it is a pointer now:
record a change in `CLAUDE.md` and nowhere else.
