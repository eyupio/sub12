# Event specs

Empty by design. When event coverage starts:

1. Add POMs under `pages/event/` (mirror `pages/league/`)
2. Add an `eventFactory` fixture under `fixtures/`
3. Add `tests/event/event-lifecycle.spec.ts` and `event-edge-cases.spec.ts`

See the top-level [e2e/README.md](../../README.md) for the walkthrough.
