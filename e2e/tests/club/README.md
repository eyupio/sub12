# Club specs

Empty by design. When club coverage starts:

1. Add POMs under `pages/club/` (mirror `pages/league/`)
2. Add a `clubFactory` fixture under `fixtures/`
3. Add `tests/club/club-lifecycle.spec.ts` and `club-edge-cases.spec.ts`

See the top-level [e2e/README.md](../../README.md) for the walkthrough.
